FROM nginx:alpine

# Copy the react frontend into the default nginx path
COPY ui/react_frontend.html /usr/share/nginx/html/index.html

# Expose port 80 (standard for nginx)
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]